server {
    listen 80;
    server_name logam.click; # 사용자님의 도메인

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}