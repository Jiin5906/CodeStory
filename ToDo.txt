  🔴 Critical — 출시 전 반드시 수정      
 1. 테스트 버튼이 프로덕션 코드에 노출됨                                                                                

  HomeView.jsx 하단에 온보딩 테스트, 투어 테스트, 레벨업 테스트 버튼이 실제 사용자에게 보입니다. 출시 전 반드시
  제거하거나 개발 환경에서만 표시되도록 해야 합니다. -> 쉬워서 바로 컷

  2. 보안: 인증 토큰 없음 -> 해야함

  현재: localStorage에 user 객체만 저장
  문제: API 직접 호출 시 누구나 다른 유저의 일기를 조회/수정/삭제 가능
       GET /api/diaries?userId=1 → 어떤 클라이언트도 접근 가능
  최소한 서버에서 userId 검증이라도 해야 합니다. 완전한 JWT는 나중에 해도 되지만, 공개 피드가 아닌 데이터는 본인만 접근할
   수 있어야 합니다.

  3. 게스트 계정 구조적 결함 -> 게스트를 아예 막는 방법으로 가야함. 로그인을 하지 않으면 이용 못하게

  handleLoginSuccess({ id: 0, nickname: '게스트' })
  모든 게스트가 id=0을 공유합니다. 게스트 A의 일기가 게스트 B에게 보일 수 있습니다. 게스트는 아예 쓰기 기능을 막거나,    
  임시 UUID를 부여해야 합니다.

  4. alert() 직접 사용

  // App.jsx 158번 줄
  alert('일기가 저장되었습니다!');
  브라우저 기본 alert는 UX상 매우 구식입니다. 이미 ErrorBanner 컴포넌트도 있는데 성공 토스트가 없습니다.

  ---
  🟡 Important — 가급적 수정

  5. 상점 기능 미완성 -> 상점 기능을 완료해주셈.

  ShopPage.jsx는 "상점 준비 중" 화면입니다. 그런데 StoreView.jsx, StoreContext.jsx, StoreData.js가 이미 구현되어 있고    
  대쉬보드에서 화분/가구 장착도 됩니다. 상점 라우트(/shop)가 실제 StoreView로 연결되지 않은 상태입니다.

  6. 린트 오류 39개 -> 마지막에 고칠거임

  15 errors, 24 warnings
  Fast refresh, setState in effect, unused vars 등이 포함됩니다. 빌드는 되지만 런타임 버그의 잠재 원인입니다.

  7. DB 마이그레이션 없음 -> 바꿔주셈

  ddl-auto=update
  프로덕션에서 update는 컬럼 추가는 되지만 삭제/변경은 안 됩니다. 스키마가 꼬이면 추적이 어렵습니다. 최소한 validate로   
  바꾸고 수동 마이그레이션을 관리하는 것이 안전합니다.

  8. LevelUpModal.jsx가 미커밋 상태 -> 레벨업은 현재 mvp넣을 것이 아님 미완으로 남겨도 무방

  git status에 ?? LevelUpModal.jsx, ?? MongleIcons.jsx가 untracked로 남아 있습니다. 레벨업 기능이 완성됐는지 미완성인지  
  불확실합니다.

  ---
  🟢 Nice to Have — 여유 있을 때

  9. 온보딩에서 수집한 데이터 미활용

  성별(gender), 유입 경로(channel)를 수집하지만 백엔드로 전송하지 않습니다. GTM 이벤트로만 보내고 있어서 CRM/분석에      
  활용이 제한됩니다.

  10. 모바일 라우트에 상점/피드 없음 -> 

  // App.jsx - 모바일 라우트
  <Route path="dashboard" element={<HomeView />} />
  <Route path="diary" element={<DiaryView />} />
  <Route path="calendar" element={<CalendarView />} />
  <Route path="stats" element={<ReportView />} />
  <Route path="settings" element={<SettingsView />} />
  // ← 상점, 피드, 일기 상세 없음

  11. Google OAuth 미연결

  SecurityConfig.java에 설정은 되어 있지만 로그인 UI에 소셜 로그인 버튼이 없습니다.

2, 3, 4, 5, 7번을 우선적으로 해결 제가 중간에 -> 화살표를 통해 원하는 바를 남겨 놨으니 이걸 참고해서 작업 진행. 8번은 신경쓸 사항이 아님. 남은 것 1, 9, 10, 11임. 1번은 가장 마지막에 진행, 9번은 성별, 유입경로를 내가 직접 관리할 수 있도록 해주면 좋겠음. 저것의 목적은 데이터를 뽑아서 보기 위한 것이기 때문에 한 눈에 확인할 수 있도록 해주면 좋겠음. 10번은 내일 서버 키고 커밋한 뒤 확인, 11번은 연결 되어 있지 않나? 안 되어 있으면 로그인 자체가 안 될텐데 잘 되는것을 보아하니 뭔가 오류가 있는 듯. 마지막은 1번을 지우고 6번에서 모든 오류를 해결한 뒤, 모바일 화면으로 보일 수 있도록 하는 것이 관건. 반드시 이번주내로 mvp가 나와야함. 그래야 다음 프로젝트를 진행할 수 있음.